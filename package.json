{
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && cp -r public dist && node src/generate-homepage.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "mustache": "^4.2.0"
  },
  "prettier": {
    "semi": false
  }
}
